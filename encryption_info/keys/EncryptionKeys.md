# Encryption Information
This folder and the parent folder contains data pertain to chats between users as well as identifying encryption information.
As of now this directory there should be files `private.pem` and `public.pem`. These are currently not in use, though will be in the future
To view your unencrypted private and public keys use:
```bash
cat private.pem; cat public.pem
```
